@page
@model Darl.GraphQL.Pages.boteditorModel

<div class="space-70"></div>
<div class="container margin-b-40">
    <div class="row">
        <div class="col-md-12 margin-b-40">
            <h1>Bot model editing</h1>
            <p>Select a bot model from your account to edit.</p>
            <label>bot models </label>
            <select id="botmodel-dropdown" name="botmodel" class="dropdown"></select>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12 margin-b-40">
            <div class="container">
                <div class=" = row">
                    <h4>Edit Phrases</h4>
                </div>
                <div class="row">
                    <div class="col-md-2">
                        <label>Find phrase</label>
                    </div>
                    <div class="col-md-8">
                        <input class="input form-control" id="findtext" type="text" value="">
                    </div>
                    <div class="col-md-2">
                        <input class="checkbox" type="checkbox" id="phraseexists" readonly disabled="disabled"><label for="phraseexists"> Exists</label>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-2">
                        <label id="patternLabel">Pattern</label>
                    </div>
                    <div class="col-md-8">
                        <input class="input  form-control" id="pattern" type="text" value="">
                    </div>
                </div>
                <div class="row">
                    <br />
                    <div class="btn-group" role="group">
                        <button type="button" class="btn btn-info" id="addbutton" disabled>Add</button>
                        <!--<button type="button" class="btn btn-info" id="mergebutton">Merge</button>-->
                        <button type="button" class="btn btn-danger" id="deletebutton" disabled>Delete</button>
                        <!--<button type="button" class="btn btn-info" id="separatebutton">Separate</button>-->
                    </div>
                </div>
                <div class="row">
                    <label><a data-toggle="collapse" data-target="#patternhelp">Help with patterns</a></label>
                    <div class="well collapse" id="patternhelp">
                        <p>
                            Define patterns by separating words with the "/" symbol.
                        </p>
                        <p>All words should be lower case only.</p>
                        <p>Separate alternate choices with the "|" symbol.</p>
                        <p>So, <pre>talk/to/dr|doctor|doc/andy</pre> will match "talk to dr andy" and "talk to doctor andy" and "talk to doc andy"</p>
                        <p>Concepts and special symbols have a ":" character in them.</p>
                        <p>So, <pre>my/name/is/value:</pre> will match "my name is andy" and extract the "andy" as the value.</p>
                        <p>Defaults are written as "default:" so <pre>who/is/default:</pre> will match "who is andy" if "andy" is not specified in another phrase.</p>
                    </div>
                </div>
            </div>
            <div >
                <h4>Attributes for <i><span id="nodePathText"></span></i></h4>
                <!-- Nav tabs -->
                <ul class="nav nav-tabs" role="tablist">
                    <li class="active"><a href="#response" role="tab" data-toggle="tab">Response</a></li>
                    <li><a href="#calltab" role="tab" data-toggle="tab">Call</a></li>
                    <li><a href="#advanced" role="tab" data-toggle="tab">Advanced</a></li>
                </ul>

                <!-- Tab panes -->
                <div class="tab-content">
                    <div class="tab-pane active" id="response">
                        <div id="responseedit">

                        </div>
                    </div>
                    <div class="tab-pane" id="calltab">
                        <div id="calledit">

                        </div>
                    </div>
                    <div class="tab-pane" id="advanced">
                        <div>
                            <label>Darl code</label>
                            <textarea id="code"></textarea>
                        </div>
                        <hr />
                        <div id="roleEdit">
                            <label for="roles">Limit access using authorizations</label><br />
                            <select id="roles"></select>
                        </div>

                    </div>
                </div>
                <div>
                    <button class="btn btn-info pull-right" id="saveAttributes">Save attributes</button>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12 margin-b-40">
            <button id="Save" class="btn btn-primary" onclick="download('test.txt', 'Hello world!');">Save</button>
        </div>
    </div>
</div>

@section StyleSheets
{
    <link rel="stylesheet" href="/codemirror/theme/neat.css" />
    <link rel="stylesheet" href="/codemirror/codemirror.css" />
    <link rel="stylesheet" href="/codemirror/addon/display/fullscreen.css" />
    <link rel="stylesheet" href="/codemirror/addon/hint/show-hint.min.css" />
    <link rel="stylesheet" href="/codemirror/addon/lint/lint.css" />
    <link rel="stylesheet" href="/codemirror/addon/fold/foldgutter.css" />
}

@section Scripts
{
    <script src="jquery/jquery.min.js"></script>
    <script src="/codemirror/codemirror.js"></script>
    <script src="/codemirror/mode/javascript/javascript.js"></script>
    <script src="/codemirror/addon/display/fullscreen.js"></script>
    <script src="/codemirror/addon/hint/show-hint.js"></script>
    <script src="/codemirror/addon/fold/foldcode.min.js"></script>
    <script src="/codemirror/addon/fold/foldgutter.min.js"></script>
    <script src="/codemirror/addon/fold/brace-fold.min.js"></script>
    <script src="/codemirror/addon/lint/lint.js"></script>
    <script src="/codemirror/addon/lint/json-lint.min.js"></script>
    <script src="/darl-codemirror/darl-codemirror.js"></script>
    <script src="/graphql/graphql.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>


    <script>
        var darlCodeMirror;
        var timeout;
        $(async function () {
            darlCodeMirror = CodeMirror.fromTextArea(document.getElementById("code"), {
                matchBrackets: true,
                autoCloseBrackets: true,
                lineNumbers: true,
                theme: 'neat',
                mode: "text/x-darl",
                gutters: ["CodeMirror-lint-markers"],
                extraKeys: {
                    "Ctrl-Space": "autocomplete",
                    "F11": function (cm) {
                        cm.setOption("fullScreen", !cm.getOption("fullScreen"));
                        $('.navbar').hide();
                    },
                    "Esc": function (cm) {
                        if (cm.getOption("fullScreen")) cm.setOption("fullScreen", false);
                        $('.navbar').show();
                    }
                },
                lint: {
                    "getAnnotations": CodeMirror.remoteValidator,
                    "async": true,
                    "check_cb": check_syntax
                }

            });
            darlCodeMirror.setValue(convertescapes('ruleset myfirstruleset \n{ \n}'));
            darlCodeMirror.setSize(1200, 800);

            darlCodeMirror.on("inputRead", function (cm) {
                if (timeout) clearTimeout(timeout);
                timeout = setTimeout(function () {
                    CodeMirror.showHint(cm, CodeMirror.hint.darl, { completeSingle: false });
                }, 150);
            });

            darlCodeMirror.on("keyup", function (cm, event) {
                var keyCode = event.keyCode || event.which;
                if (keyCode == 8 || keyCode == 46) {
                    if (timeout) clearTimeout(timeout);
                    timeout = setTimeout(function () {
                        CodeMirror.showHint(cm, CodeMirror.hint.darl, { completeSingle: false });
                    }, 150);
                }
            });

            var dropdown = $('#botmodel-dropdown');
            var rsname;
            dropdown.empty();
            dropdown.append('<option selected="true" disabled>Choose a ruleset to edit</option>');
            dropdown.prop('selectedIndex', 0);

            var graph = graphql("https://darl.dev/graphql");
            var allbotmodels = graph(`{ botmodels { name }}`);
            var darlContent = graph(`query getdarl($name: String!){  getDarlFromRuleSet(ruleSetName: $name)}`);
            var savecontent = graph('mutation setdarl($name: String!, $darl: String!){updateDarlInRuleset(ruleSetName: $name, darl: $darl)}');
            const rs = await allbotmodels();
            $.each(rs.botmodels, function (key, entry) {
                dropdown.append($('<option class="dropdown-item"></option>').attr('value', entry.name).text(entry.name));
            });

            $('#botmodel-dropdown').on('change', async function () {
                rsname = this.value;
                var content = await darlContent({ name: rsname });
                darlCodeMirror.setValue(convertescapes(content.getDarlFromRuleSet));
            });
            $('#Save').click(async function () {
                var content = $("#Darl").val();
                await savecontent({ name: rsname, darl: content });
            });


        });

        function check_syntax(code, result_cb) {
            var baseUri = "https://darl.ai/api/Linter/DarlLint";
            var jqxr = $.post(baseUri, { 'source': code }, function (data, status, xhr) {
                result_cb(data);
            });

        }
        function convertescapes(text) {
            text = text.replace(/&quot;/g, '"').replace(/&#39;/g, "'").replace(/&gt;/g, '>').replace(/&lt;/g, '<').replace(/&#x221E;/g, '∞').replace(/&#xD;/g, '\r').replace(/&#xA;/g, '\n').replace(/&#x9;/g, '\t').replace(/&#x2B;/g, '+').replace(/&#x27;/g, '\'');
            return unescape(text);
        }

    </script>
}